%このファイルでは様々なパッケージを読み込んでいる。
%実際に私の修論をで利用したものをそのまま掲載しているので、
%必要に応じて増やしたり減らしたりする必要がある。

\usepackage{graphicx}
%図を入れるのに必要。

\usepackage{xcolor}
%凝った色使いに必要（おそらく通常の修論では不要）。

\usepackage{amsmath,amssymb}
\usepackage[ppl]{mathcomp}
%数学記号がいろいろ使えるようになる。

\usepackage{layout}
%本のレイアウトを表示することができる。
%レイアウトを設定し終えたら不要。

\usepackage{wrapfig}
%図を本文の横に表示できるようになる。
%好みに応じて使ってもよい。

\usepackage{bm}
%太字のベクトル表記に必要

\usepackage{comment}
%コメントアウトができるようになる。
%地味によく使う。

\usepackage{tikz}
%図を作れるようになる。
%tikzで論文に載せる図を作るのはお勧めしないが、
%体裁のデザインを作るのには必要。

\usepackage{float}
%図を表示する位置の制御ができるようになる。

\usepackage{array}
%表を作るのに便利。

\usepackage{cite}
%参考文献1,2,3を引くときに[1-3]のように表記してくれるようになる。

\usetikzlibrary{fadings}
%tikzでグラデーションの入った凝った図を作るときに使える。
%普通の修論では絶対に不必要だが、私の趣味で入れておいた。